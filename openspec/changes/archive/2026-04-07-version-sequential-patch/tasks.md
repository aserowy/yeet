## 1. Update Version Computation

- [x] 1.1 Replace the day-based version computation in `automated-release.yml` with sequential patch logic: list tags for current year-month, find highest patch, increment by one (or start at 0)

## 2. Documentation

- [x] 2.1 Update `docs/releasing.md` to reflect the new `vYYYY.M.PATCH` versioning scheme
