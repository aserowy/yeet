## 1. Fix sed scope

- [x] 1.1 Replace the blanket `sed` command in `automated-release.yml` with two targeted replacements: one for the `[workspace.package]` version line and one for `yeet-*` internal crate entries
